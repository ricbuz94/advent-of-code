const { getFileLines } = require("../utils");

function start(filePath) {

  let res;
  const lines = getFileLines(filePath);

  // Part #1


  // Part #2
  

  console.log("\nres: %s", res);
}

start('input.txt');